#include <bits/stdc++.h>
using namespace std;
int main() {
    int arr [2];
    cin >> arr[0] >> arr[1];
    arr[1] += 32;
    cout << arr[-2] << endl;
    int arr2 [3][5];
    string s = "Soumyajit";
    s[0] = ' A' works but s[0] = "A" wont.
    int len = s.size();
    cout << s[len-1];
    
}
